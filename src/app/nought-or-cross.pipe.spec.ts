import { NoughtOrCrossPipe } from './nought-or-cross.pipe';

describe('NoughtOrCrossPipe', () => {
  it('create an instance', () => {
    const pipe = new NoughtOrCrossPipe();
    expect(pipe).toBeTruthy();
  });
});
